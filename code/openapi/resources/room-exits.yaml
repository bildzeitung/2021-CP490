get:
  summary: Retrieve exits for a given room
  tags:
    - exit
  parameters:
    - $ref: '../parameters/path/roomId.yaml'
  responses:
    '200':
      $ref: '../responses/ExitListResponse.yaml'
post:
  summary: Add a new exit to a room
  tags:
    - exit
  parameters:
    - $ref: '../parameters/path/roomId.yaml'
  requestBody:
    $ref: '../requestBodies/ExitBody.yaml'
  responses:
    '201':
      $ref: '../responses/RoomExitDetailResponse.yaml'