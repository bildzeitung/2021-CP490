get:
  summary: Retrieve a list of events
  tags:
    - event
  parameters:
    - $ref: '../parameters/path/gameId.yaml'
  responses:
    '200':
      $ref: '../responses/EventListResponse.yaml'
post:
  summary: Create a new event
  tags:
    - event
  parameters:
    - $ref: '../parameters/path/gameId.yaml'
  requestBody:
    $ref: '../requestBodies/EventBody.yaml'
  responses:
    '201':
      $ref: '../responses/EventDetailResponse.yaml'