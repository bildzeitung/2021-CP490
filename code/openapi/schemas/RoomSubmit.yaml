allOf:
  - $ref: './ObjectBase.yaml'
  - type: object
    properties:
      game_id:
        type: string
      description:
        type: string
      exits:
        type: array
        items:
          $ref: './ExitSubmit.yaml'
      properties:
        type: object
        additionalProperties:
          type: string